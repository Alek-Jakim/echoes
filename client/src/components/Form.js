import React from 'react'
import useStyles from '../styling/formStyles';

const Form = () => {
    const classes = useStyles();
    return (
        <>
            <h1>FORM</h1>
        </>
    )
}

export default Form
