import React from 'react'
import useStyles from '../styling/postStyles';

const Post = () => {
    const classes = useStyles();

    return (
        <div>
            Post
        </div>
    )
}

export default Post
